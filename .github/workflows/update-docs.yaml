name: Update Docs

on:
  push:
    branches: ["dev"]

jobs:
  update-docs:
    name: Update Directory Tree
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Gerar Árvore de Diretórios
        run: |
          npx tree-cli@latest -d -l 4 --ignore "node_modules, .git, dist, coverage, build, .turbo, uploads" > tree.txt

      - name: Injetar Árvore no Markdown
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            const tree = fs.readFileSync('tree.txt', 'utf8');
            const readmePath = 'README.md';
            let readme = fs.readFileSync(readmePath, 'utf8');

            const startTag = '<!-- START_TREE -->';
            const endTag = '<!-- END_TREE -->';

            const regex = new RegExp(`${startTag}[\\s\\S]*?${endTag}`, 'g');
            const newContent = `${startTag}\n\`\`\`bash\n${tree}\`\`\`\n${endTag}`;

            readme = readme.replace(regex, newContent);
            
            fs.writeFileSync(readmePath, readme);

            fs.unlinkSync('tree.txt');

      - name: Criar Pull Request Automático
        if: ${{ !env.ACT }}
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "docs: atualiza árvore de diretórios"
          branch: chore/update-docs-tree
          delete-branch: true
          title: "docs: Atualização Automática da Árvore de Diretórios"
          body: |
            🤖 **Atualização Automática**
            Este PR foi gerado automaticamente pelo pipeline de CI/CD.
            Foram detectadas mudanças na estrutura do diretório `src/` e o `README.md` foi atualizado para refletir a nova arquitetura.
          labels: documentation, automated pr
