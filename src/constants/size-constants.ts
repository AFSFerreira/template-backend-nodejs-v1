export const KB_IN_BYTES = 1024
export const MB_IN_BYTES = 1024 * KB_IN_BYTES

export const MAX_USER_PROFILE_IMAGE_FILE_SIZE_BYTES = 5 * MB_IN_BYTES // Tamanho limite de 5mb para as imagens de perfil dos usuários comuns

export const MAX_DIRECTOR_BOARD_PROFILE_IMAGE_FILE_SIZE_BYTES = 15 * MB_IN_BYTES // Tamanho limite de 15mb para as imagens de perfil dos membros do corpo diretivo

export const MAX_INSTITUTIONAL_ABOUT_IMAGE_FILE_SIZE_BYTES = 30 * MB_IN_BYTES // Tamanho limite de 30mb para a imagem da página institucional

export const MAX_SLIDER_IMAGE_FILE_SIZE_BYTES = 30 * MB_IN_BYTES // Tamanho limite de 30mb para imagens de slider

export const MAX_MEETING_BANNER_IMAGE_FILE_SIZE_BYTES = 15 * MB_IN_BYTES // Tamanho limite de 15mb para imagens de banner de reuniões

export const MAX_BLOG_BANNER_FILE_SIZE_BYTES = 15 * MB_IN_BYTES // Tamanho limite de 15mb para imagens de banner de blogs

export const MAX_BLOG_IMAGE_FILE_SIZE_BYTES = 10 * MB_IN_BYTES // Tamanho limite de 10mb para imagens de imagens de blogs

export const MAX_DOCUMENT_FILE_SIZE_BYTES = 30 * MB_IN_BYTES // Tamanho limite de 30mb para os documentos genéricos enviados

export const MAX_MEETING_AGENDA_FILE_SIZE_BYTES = 30 * MB_IN_BYTES // Tamanho limite de 30mb para os documentos de programa de reuniões

export const MAX_NEWSLETTER_HTML_FILE_SIZE_BYTES = 5 * MB_IN_BYTES // Tamanho limite de 5mb para os arquivos HTML das newsletters

export const SEARCH_CONTENT_TEXT_LIMIT = 600 // Tamanho limite de 600 caracteres para o conteúdo de pesquisa (evitar indexar muito conteúdo)
