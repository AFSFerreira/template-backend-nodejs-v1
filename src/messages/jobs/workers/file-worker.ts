import type { ISystemResponse } from '@custom-types/responses/system-response'

export const INVALID_FILE_OPERATION_TYPE: ISystemResponse = {
  code: 'INVALID_FILE_OPERATION_TYPE',
  message: `Tipo de operação de arquivo inválido`,
}
